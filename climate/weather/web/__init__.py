"""The read-only weather HTTP API and dashboard static files.

See :mod:`climate.weather.web.api` for the route handlers and the thin
``http.server`` wrapper, and ``docs/weather-api.md`` for the contract they
implement. ``python -m climate.weather.web`` (see
:mod:`climate.weather.web.__main__`) starts the service.
"""

from __future__ import annotations
